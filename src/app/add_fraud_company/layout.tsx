import SharedLayout from "../SharedLayout";

export default function AddFruadCompaniesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <SharedLayout className={""}>{children}</SharedLayout>;
}
